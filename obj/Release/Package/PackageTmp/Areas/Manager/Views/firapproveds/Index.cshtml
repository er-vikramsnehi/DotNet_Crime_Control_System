@model IEnumerable<cmspro.Models.firapproved>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.fir_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fir_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.police_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.approve)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.fir_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fir_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.police_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.approve)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.fir_approved_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.fir_approved_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.fir_approved_id })
        </td>
    </tr>
}

</table>
